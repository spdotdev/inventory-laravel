{{-- Web parity Task 1: toast container for web-feedback.js. Success/error
     toasts (including the failure Retry action) are appended here by JS;
     empty and inert on pages that never call InventoryFeedback.save(). --}}
<div id="inv-toast-container" class="inv-toast-container" aria-live="polite"></div>
